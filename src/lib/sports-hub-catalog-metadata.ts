// Keep this lightweight so the client-only superadmin dashboard does not bundle
// the full built-in Sports Hub article database. The regression test verifies it.
export const STATIC_SPORTS_HUB_ARTICLE_COUNT = 137;
