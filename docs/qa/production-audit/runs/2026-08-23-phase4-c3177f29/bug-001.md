# BUG-001 independent verification — BLOCKED

**Run:** 2026-08-23 phase 4, independent local-only replay
**Base:** `34c5aa2c24ebb6e70e52b4aaeb4b1ac69c1244db`
**Result:** `BLOCKED` — the required fresh browser UI replay could not be completed with the supplied local browser/toolchain combination. This record does not mark BUG-001 fixed.

## Independent code review

- `EventDetailDialog.tsx` provides the event object and deletion callback only through `EventDeleteConfirmation`.
- `EventDeleteConfirmation.tsx` keeps its dialog closed initially, opens the labelled destructive control without calling the callback, names the selected event in the alert-dialog text, and exposes Cancel separately from confirmation.
- A per-open-cycle confirmation guard calls the callback once, resets on reopening, and therefore permits one new confirmation in the next cycle.
- The source regression asserts that the initial delete control is not directly wired to the mutation. The rendered regression covers Cancel, one confirmation despite a duplicate click, and a new confirmation after reopening.

## Focused automated evidence

| Check | Result |
| --- | --- |
| Node source-contract command | PASS — 1 matching test passed; 0 failures. |
| Original brief-specified Vitest command | Corrected plan-command issue — exited successfully but selected 0 tests because its `requires explicit confirmation` filter does not match the checked-in test name; this is not a product failure. |
| Rendered behavior test with its actual checked-in name | PASS — 1 test passed; 0 failures; 1 unrelated test skipped. |

The controller ruled the stale filter a plan-command issue. The separately run rendered behavior test, using its exact checked-in name, verifies the intended contract.

## Browser replay attempt

An isolated local Auth/Firestore/Storage emulator suite and local app server were prepared for the supported ephemeral anonymous Squad Pro flow. The browser could not reach its first stable snapshot, so no anonymous demo session or synthetic event was created and no demo-data cleanup was required.

The required bundled Playwright CLI was run in a unique `phase4_bug001_20260823` session. The locally present WebKit executable is build `webkit-2336`, while the bundled CLI revision requires build `webkit-2342`. No browser-download command was run. A local executable-path compatibility attempt created a WebKit session but every snapshot invocation failed to return before the command timeout, so no stable element reference or UI interaction was possible.

Chromium and Firefox remain unavailable locally. The controller corrected the browser-selection path: the in-app Browser binding was unavailable, but URL-target selection found a connected Chrome browser and reached the local login page. The authorized fresh Squad Pro demo launch then failed visibly with `Demo Launch Failed` and `Verification service unavailable. Try again shortly.` before the workspace loaded.

Systematic diagnosis established the failing boundary. The synthetic client web configuration, emulator flag, client-derived Auth/Firestore/Storage hosts, server emulator hosts, and emulator project were all configured consistently for the same synthetic demo project. The client logged that emulator connections were enabled. The local process could reach Auth and Firestore emulators successfully, but the selected browser's direct Auth-emulator probe failed with `net::ERR_BLOCKED_BY_CLIENT`. Its console simultaneously recorded repeated Firestore `WebChannelConnection RPC 'Listen' ... transport errored` warnings at `src/firebase/client-provider.tsx`; a local layout-chunk `SyntaxError: Invalid or unexpected token` was also observed. That syntax error was not reproduced against the repaired event UI and is not classified as a BUG-001 result. After the successful session-clear request, the local server received no demo-seed or session-creation request. Therefore `signInAnonymously` fails at the browser-to-Auth-emulator boundary and never obtains an ID token for `/api/demo/seed` or `/api/auth/session`.

No event could be created or selected. Accordingly, this task cannot supply the required fresh browser evidence at either requested viewport without a browser context that can reach the local Auth/Firestore/Storage emulator ports, which is outside this run's authorization.

| Browser/request evidence | Count | Status |
| --- | ---: | --- |
| Initial local app-page load | 1 | 200 |
| Event-action requests before confirmation | Not measurable | No event workspace loaded |
| Event-action requests after confirmation | Not measurable | No event workspace loaded |
| Local harness console observation | 1 observed | Layout-chunk syntax error; not reproduced against repaired event UI and not a BUG-001 result |

No raw network output, trace, storage state, or screenshot was retained. Transient local emulator logs and the temporary Playwright configuration were removed after the attempt.

## Required follow-up

Provide a browser context that can reach the synthetic local Auth, Firestore, and Storage emulator ports (or a bundled-CLI-compatible local WebKit build) and rerun the full anonymous-demo flow: 390×844 and 1440×900, pre-confirm request count, Cancel/reload persistence, Confirm/reload deletion, dialog focus/Escape/Cancel focus return, destructive label, overflow, console, and one-confirm-per-open-cycle request count.
